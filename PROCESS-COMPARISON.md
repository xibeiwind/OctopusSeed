# 过程横向评估：OctopusSeed 与常见软件开发过程

> **本文档是什么**：把 OctopusSeed 所描述的开发过程与主流过程 / 框架做一次横向评估，回答三个问题——
> ① 它属于什么传统；② 它在哪些维度上强、哪些维度上弱；③ 什么场景该用、什么场景别用。
> **定位**：评估**快照**（2026-09-15，对应 `manifest.json` v1.0.0）。机制描述**不复述规范正文**，只引用条目
> （`template/CONTRIBUTING.md` 的 § 号与 `R-Plan-x`）——避免同一事实两处漂移（承 `R-Plan-6`）。
> **读者**：决定是否采用本模板的人；以及采用后想知道"还需补什么"的人。
> **证据等级**：本文区分两类结论——**机制分析**（读规范即可判定）与**运行证据**（来自模板来源项目的实际执行记录，见附录 B）。

---

## 1. 定性：它是三种传统的混合体 + 一次 AI 时代改造

| 传统 | 本过程借了什么 | 改造点 |
|---|---|---|
| 计划驱动（瀑布 / V 模型 / RUP / CMMI） | 阶段 + 工作包 + 需求↔代码双向追溯 + 证据链 | 阶段**滚动收口**（非一次成型）；追溯**用脚本校验**而非人工审计 |
| 流式 / 看板（Kanban） | 连续流、无迭代时间盒、拉式（读计划 → 认领包） | WIP 限制**定性**（"只跑关键路径"），非定量上限 |
| Shape Up | **容量封顶 + 超了挤掉一个** + 刻意未做清单 + 冷却期（收口评审） | 周期单位是"阶段"，不是固定 6 周 |
| **过程即代码**（新增） | — | 约束编进**编译错误 / git 钩子 / CI 脚本**（`template/CONTRIBUTING.md` §8），不依赖人的纪律 |

**最接近的三个对照物**：`Shape Up`（范围控制哲学）、`轻量 CMMI`（追溯与证据）、`spec-driven 开发`（GitHub Spec Kit / Kiro 一类，
把规范喂给 AI）。与第三者的**关键区别**：多数 AI 工具链做的是"让 AI 更懂项目"（上下文工程），
本过程做的是"**让 AI 的输出可审计、可阻断**"。

---

## 2. 主比较表

### 2.1 计划驱动族

| 维度 | 瀑布 / V 模型 | RUP | CMMI | **OctopusSeed** |
|---|---|---|---|---|
| 节奏单位 | 阶段闸口（一次成型） | 4 阶段 × 迭代 | 过程域 | **阶段 Px（不定长）+ 工作包** |
| 工作单元 | WBS 任务 | 用例 / 活动 | 过程域 | **工作包 Px-y（≤8 / 阶段）** |
| 需求来源 | 一次性基线 | 用例模型 | 基线 | **`docs/01` + 阶段计划 §3** |
| 范围控制 | 变更控制委员会 | CCB | 基线控制 | **容量封顶 + 超出必须挤掉（`R-Plan-9`）** |
| 门禁 | 人工评审 | 里程碑评审 | 审计 | **编译错误 + 钩子 + CI（§8 三层）** |
| 追溯 | 需求矩阵（纸面） | 用例实现矩阵 | **强（审计级）** | **强（脚本校验：`governance-check`）** |
| 度量 | 挣值（EVM） | EVM | 过程能力 | **缺失（见 §6）** |

### 2.2 迭代 / 流派

| 维度 | Scrum | Kanban | XP | Shape Up | SAFe | DevOps / Trunk | **OctopusSeed** |
|---|---|---|---|---|---|---|---|
| 时间盒 | **有（硬）** | 无 | 有 | 有（6 周） | 有（PI） | 无 | **无** |
| 工作单元 | Product Backlog Item | 卡片 | 用户故事 | Pitch | Feature | 变更集 | **工作包 Px-y** |
| "下一步"出处 | Sprint Backlog | 队列顶端 | 故事墙 | Betting table | PI 目标 | 需求队列 | **阶段计划 §3/§4（唯一入口，`R-Plan-3`）** |
| 范围控制 | Velocity 吸收 | 队列无限 | 计划游戏 | **Appetite 封顶 + 砍 scope** | 容量规划 | 无 | **容量封顶 + 候选池** |
| 中途新需求 | 下个 Sprint | 入队列 | 下个迭代 | 不打断 cycle | 下个 PI | 随时 | **入候选池，只在收口摘取（`R-Plan-8`）** |
| 门禁 | DoD（靠自觉） | 无 | **CI + TDD** | 无 | 检查点 | **流水线** | **编译/钩子/CI + 文档引用/编号登记** |
| 度量 | Velocity / 燃尽 | 周期时间 / CFD | — | 无 | 各类 KPI | **DORA 四指标** | **缺失** |

### 2.3 AI 原生族

| 维度 | 主流 spec-driven（Spec Kit / Kiro / AGENTS.md） | **OctopusSeed** |
|---|---|---|
| 目标 | 给 AI 更好上下文 → 产出更准 | 给 AI 约束 + 给人类审计入口 |
| 规范形态 | 提示词 / 规格文件（AI 读） | 契约文档 + **可执行门禁**（CI 读） |
| 失误防线 | 靠 AI 自觉遵守指令 | **预防层（AI 规则）+ 强制层（编译/钩子/CI）分离** |
| 多会话连续性 | 依赖上下文重灌 | **阶段计划 §3/§4 是落盘交接点**（会话无关） |
| 文档腐化 | 无机制 | 引用可解析 + 编号登记 + 单一权威处 |

---

## 3. 六项深入对比

### 3.1 范围控制：比 Scrum 硬，接近 Shape Up
Scrum 靠 **Velocity 吸收**变化（"这次做不完下个 Sprint"），本质是**软化**而非拒绝。本过程用 **容量封顶（§9.1 `R-Plan-9`）+
"超出必须挤掉一个，而不是追加"**，把取舍变成**强制的单次决策**；再配**去哪儿的单一登记**（`范围边界清单.md`，`R-Plan-7`——
答不出触发条件即 Q4，永不排期）。这与 Shape Up 的 Appetite 同源，但多了**登记处不设上限、排期表必须封顶**的双清单结构。

### 3.2 追溯与门禁：CMMI 的追溯 + DevOps 的强制，但**校验对象不同**
CMMI 的追溯靠**审计**（人查）；DevOps 的门禁只覆盖**构建与测试**。本过程把强制力用在 CMMI 关心的东西上：
`tools/governance-check.ps1` 校验提交前缀、**文档相对引用是否可解析**、编号是否登记（拦截"幽灵包"）。
代价是：它校验**形态**（前缀对不对、引用存不存在），**不校验语义**（这个包是否真做完）。

### 3.3 门禁的元规则：**过程自身可被测试**
§10 ⑫ 要求"**门禁写完必须拿一个已知缺陷去撞一次**"，且本仓真的执行过（附录 B）。主流框架里没有这条——
Scrum/SAFe 的 DoD、CMMI 的审计都不要求"证明你的门禁有牙"。**这是本过程最被低估的部分**：它把"过程有效性"从信念变成实验。

### 3.4 人机分工是 RACI 的重写，不是 RACI 的应用
既有框架都假定**人执行流程**；本过程假定 **AI 承担机械劳动（读计划、回填、写文档、对齐前缀），人只做决策与合并批准**。
因此它敢要求"文档重 + 回填勤 + 引用严格"——这些在纯人工团队里会被视为官僚负担（这正是它自承的"规范较重是刻意的"）。
**它的重量与它的前提绑定**：前提消失，重量就成了纯成本（故 `MANIFEST.md` §3 给出三种用法，含"只读清单"）。

### 3.5 时间盒缺失——最需正视的结构性差异
Sprint / cycle 提供两件事：**强制检查点**与**可预测节奏**。本过程以"阶段"替代，但**阶段长度不定**
（来源项目的 P0 跨多日仍在进行）。因此：
- 没有周期性"计划是否仍成立"的强制复盘（`R-Plan-8` 只在**阶段收口**发生）；
- 风险链是 **阶段拖长 → 计划陈旧 → 唯一开工入口失效**（回填机制缓解，但不根治）。
- 缓解现状：看板已有"文档新鲜度"阈值（`STALE_DAYS`），但**只读不强制**。

### 3.6 与 AI 原生工具链的互补性
两者不冲突：spec-driven 工具解决"AI 读什么"，本过程解决"AI 产出如何被验证与交接"。
理想组合是 **spec-driven 做输入、本过程做输出侧门禁与计划交接**——本过程对此不设障碍（`docs/01` / `docs/02` 即规格文件的天然落点）。

---

## 4. 强度快照

| 能力 | 评价 | 依据 |
|---|---|---|
| 范围控制 / 防膨胀 | **强** | 容量封顶 + 候选池 + 触发条件必答 |
| 追溯性 | **强** | 三层编号 + RTM + `registry` 门禁 |
| 门禁强制力（机械层） | **强** | 编译错误 / 钩子 / CI 三处拦截 |
| 门禁强制力（语义层） | **弱** | 不验证"包是否真做完"，靠人审 |
| 文档可持续性 | **强** | 单一权威处 + 引用校验 + 回填同批提交 |
| 变更管理 | **强** | 中途入池、只在收口摘取 |
| AI 协作适配 | **强** | 前提即人机分工；阶段计划是会话无关的落盘入口 |
| 需求价值排序 | **中** | 象限是客观判据（§9.5.1），但无价值量化 / 成本估算 |
| **效率度量（周期 / 吞吐 / 失败率）** | **缺失** | 只记门禁数字，无 DORA 类指标 |
| **迭代节奏 / 检查点频率** | **弱** | 无时间盒，检查点只在阶段收口 |
| **团队 / 多角色扩展性** | **弱（未验证）** | 只有"单人 + AI"的运行证据；无角色、无会议机制、无多人并行冲突规则 |
| 上手成本 | **偏高** | 文档重 + 需工具链（PowerShell / CI / hooks） |

---

## 5. 三条独特之处（附运行证据）

1. **过程即代码**：把过程纪律下沉为编译错误 / 钩子 / 脚本——可执行、可阻断、可回归。业界多数框架的过程是**人的约定**。
2. **文档成为可校验资产**：单一权威处 + 引用可解析 + 编号登记，直击"文档腐烂"这一文档驱动流程的通病。
3. **门禁需自证有牙**：强制"用已知缺陷撞一次"，把有效性从信念变为实验（附录 B 有实例，含一次"假守卫"的发现）。

---

## 6. 缺口与补强建议（按性价比排序）

> **本节已经过一次评审（2026-09-15）**：逐条复核准则为**病灶驱动 / 同源可行 / 抗形式合规 / 不制造门禁疲劳 / 接口联动已计 / 可自证**。
> 结论有实质改动：**新增 2 条（G8 / G9）、2 条降级（G2 / G4）、3 条改写（G1′ / G3′ / G6′）**；改动理由写在每条「诊断复核」列里。

| 编号 | 主题 | 诊断复核 | 保障层级（§8.1） | 结论与落地形态 | 成本 |
|---|---|---|---|---|---|
| **G7** | 最小可用子集清单 | 成立——唯一的外部成本缺口，且此前没回答"只想要 30% 收益时该取哪 30%" | 硬（既有自检自动覆盖） | **立即做**：`MANIFEST.md` §3.3 增清单 | 低 |
| **G6′** | **多执行体并行规则**（原"多人协作"，**重新诊断**） | 原诊断对象错了：前提是"人 + AI"，真实病灶是**多个执行体同时开工**——编号占用冲突 / 热点文档并发写 / 回填责任不明 | ① 硬 ② 无 ③ 软 | **立即做**：三条写进 `CONTRIBUTING.md` §9.6；① 由 `tools/claim.ps1` + CI `claim` 检查硬保障 | 低 |
| **G8** | **AI 产出语义标记**（新） | 新增：AI 承担机械劳动是前提，但门禁只校验**形态**；"引用正确、语句通顺、内容错"正是 AI 的典型失败模式 | 硬（可机械判） | **立即做**：`governance-check` 的 `provenance` 检查——基线文档里的「待实测」必须给出归属（谁 / 哪份文档 / 哪个编号定值） | 低 |
| **G1′** | 度量（原 DORA 四指标，**裁剪**） | 方向对、指标选错：部署频率与恢复时间对非服务形态无意义（会得到恒为 0/1 的数）；**本过程的病灶在过程自身** | 硬（可复算断言） | **修订后做**：对外 2 条（前置时间、变更失败率）+ 对内 6 条（回填及时率、计划陈旧、引用未解析、findings 与误报占比、缺口周转、幽灵包）；落 `tools/metrics.ps1` 与看板健康面板，**不做 CI 阻断** | 低-中 |
| **G3′** | WIP 判据（**改写**） | 成立，但"🔄 计数 > 阈值"会误报：等外部输入的包长期 🔄，并非并行推进 | 硬（可机械判） | **修订后做**：判据改为**依赖已满足的并行度**；依赖未满足却标 🔄 报的是"状态与依赖不符"（`R-Plan-4` 违规）——更准，也更有价值 | 低 |
| **G2′** | 计划新鲜度（**降级**） | 成立但严重性被高估：`R-Plan-2` 已要求每包完成即回填，陈旧只会发生在"合法停滞"时段（等外部输入） | 软（**有意为之**） | **修订后做**：只做看板告警，**不进 CI**；仅"陈旧且无阻塞登记"才升为 finding（本质是 G6′ 的依赖未显式化） | 低 |
| **G9 + G5** | §3 加"体量 + 价值闸门"两列（**合并**） | 成立（新）：容量用"个数"近似"工作量"，可被"拆小包"绕过；价值闸门缺列则 Q2 内部仍无法排序 | 硬（接口联动断言） | **合并后做**：一次给 §3 加两列，**同批**改 `kanban-data.ps1` 解析与 `kanban-check.ps1` 断言（改文档形态即改接口，§10） | 中 |
| **G4** | 完成证据校验（**降级**） | 缺口成立，但方案**危险**：骗过它（随便提交一次 + 填个 PR 号）比真做完便宜 → 会**鼓励**形式合规；合法的无 PR 合并会被误报 | 软（只读可见） | **降级**：不做 CI 门禁，改为看板健康 / 欠账面板提示（"PR 挂账"项已存在）；语义缺口由 G8 覆盖一部分，其余**必须承认机械可校验性有边界** | 低 |

**三分类与实施顺序**

| 批次 | 内容 | 理由 |
|---|---|---|
| **首批（立即）** | `G7` + `G6′` + `G8` | 全部**零新增执行点**、零接口联动，一天内可落地；分别补"适用面 / 并行安全 / 语义可信"三个洞 |
| 第二批 | `G1′` + `G3′` + `G2′` | 都要新增或改写门禁，按"先告警、后严格"推进（`CONTRIBUTING.md` §8.1） |
| 第三批 | `G9 + G5` | 要动文档列与看板解析 / 断言，须**单独立包** |
| **明确不做** | `G4` 的硬门禁形态、`G2` 的 CI 阻断形态 | 会诱发形式合规、或造成门禁疲劳（§8.1 第 3 条） |

> **首批的落地状态**：`G7`（`MANIFEST.md` §3.3）、`G6′`（`CONTRIBUTING.md` §9.6 + `tools/claim.ps1` + CI `claim` 检查）、`G8`（`governance-check` 的 `provenance` 检查）**已随之落地**；
> 配套的 `G1′` 度量脚本（`tools/metrics.ps1`）与"热点文档大删可见"（`hotdocs`）同批实现，作为第二批的前置。

---

## 7. 适用性判定与迁移路径

**最适合**：以 AI 为主要实现力量、需求会持续涌入、需要**可交接与可审计**的中长期单人 / 小团队项目。

**不适合**：
- 多人并行且需要对外承诺交付日期的合同型项目（无时间盒与估算）；
- 一次性脚本 / 短期原型（重量不划算，只取编号 + 提交前缀即可）。

**迁入路径（不要一次全上）**：
1. 三件起步：单一编号体系 + 阶段计划 + 提交前缀钩子；
2. 再补：文档骨架（`docs/` 四份常驻单例 + 三份模板）与 `governance-check`；
3. 最后接：看板与栈专属门禁（`variants/<stack>`）。

**迁出路径**：若团队扩到 4 人以上，先补**角色 / 会议定义与定量 WIP**，再考虑框架升级——
问题在协作定义，不在框架层级（直接上 SAFe 会放大而非解决它）。

---

## 8. 结论

**它把"过程"从人的纪律改造成了仓库里的代码，并在范围控制上做到了接近 Shape Up 的硬度。它的独特性不在任何单一维度，
而在"追溯（CMMI 式）× 强制（DevOps 式）× 人机分工（AI 原生）"三者的交汇。**

当前最大的两个真实短板：**没有度量**（无法证明自己更快），**没有时间盒与多执行体并行定义**
（只能证明自己更可追溯、更少膨胀）。两者都有低成本补强路径（`G1′` 裁剪版度量 / `G2′` 告警 / `G6′` 并行规则），
且与既有设计同源——不需要引入新范式。

**一个附带结论**：评审过程中最有价值的一条不是"补了什么"，而是**"哪些明确不做"**——
`G4` 的硬门禁与 `G2` 的 CI 阻断被判定为**做了会更糟**（诱发形式合规 / 门禁疲劳）。
能说清"什么不该硬化"，才有资格谈"约束如何被保障"。

---

## 附录 A：评估方法与局限

**方法**：① 读 `template/CONTRIBUTING.md`（§1~§11）与 `MANIFEST.md` 提取机制；② 逐维度对照主流框架的公开定义；
③ 用来源项目的**运行记录**（附录 B）校验"机制是否真的在跑"。

**局限（请一并采信）**：
- **无对照实验**：所有比较基于机制分析，不是 A/B 结果；"更快 / 更稳"的判断**未经度量验证**（这正是 G1 要解决的）。
- **单一样本**：运行证据来自一个项目、一个人 + AI 的组合，**不能外推到多人团队**。
- **快照**：评估绑定 `manifest.json` v1.0.0；规范演进后本文档可能过时——改规范时若影响结论，应同批更新本文档。

---

## 附录 B：运行证据清单

| 证据 | 内容 | 支持了本文哪条判断 |
|---|---|---|
| 双清单容量实践 | 某阶段：**6 个包位包 + 24 个"不占包位"批次** | §3.1 容量封顶既守住主线、又不污染主线 |
| 缺口台账 | 登记 **18 条**（15 条已关闭 / 3 条开放），每条带触发条件 | §3.1 / §4 候选池与触发条件机制在真实运行 |
| 门禁"有牙"的证明 | 多组**刻意破坏**均被拦下；其中一次发现门禁是"**假守卫**"（判据太松 + 复刻不出环境），修复后才真正拦住 | §3.3 过程自身可被测试 |
| 断言自身的缺陷 | 一次门禁的新增断言因正则只捕获首个匹配而**永不报警**，由破坏测试发现 | §3.3（断言也要被验证） |
| 追溯门禁 | `registry` 拦截"幽灵包"（有提交但无登记） | §3.2 |
| 文档引用门禁 | 文档引用一次性完成对账（如 86 条引用 / 0 未解析） | §3.2 / §5 |
| 机械事实单一权威处 | 测试基线由 37 → 132，只在一处维护、其它文档只引用 | §4 文档可持续性 |

> 附录 B 的数字来自模板来源项目的实际记录（见 `MANIFEST.md` §5 血缘）。**这是"过程在跑"的证据，不是"过程更好"的证据**——
> 后者需要 `G1′` 的度量才能回答。

---

## 附录 C：评审记录

| 日期 | 事项 | 结论 |
|---|---|---|
| 2026-09-15 | §6 缺口逐条评审（准则：病灶驱动 / 同源可行 / 抗形式合规 / 不制造门禁疲劳 / 接口联动已计 / 可自证） | 新增 `G8`/`G9`；`G1→G1′`、`G3→G3′`、`G6→G6′`；`G2`/`G4` **降级**；明确"不做"两项 |
| 2026-09-15 | 保障分层设计（`CONTRIBUTING.md` §8.1）：每条约束须声明**硬 / 软 / 无**三层之一 | 首批三条（`G7`/`G6′`/`G8`）落成**可机械判定**；`G4` 与 `G6′②` 明确标为软 / 无保障 |
| 2026-09-15 | 门禁自证：新门禁逐一用已知缺陷碰撞（`CONTRIBUTING.md` §10 ⑫） | `claim` / `provenance` / `hotdocs` / `metrics -Check` 各撞一次；**`provenance` 首次运行即抓到基线文档一处无归属的「待实测」** |

> **本附录的作用**：评估结论本身也会漂移，所以**评审要可追溯**——每条修订的日期与依据留在这里，而不是覆盖旧结论（承 `R-Plan-6`「同一事实只有一个权威处」）。
