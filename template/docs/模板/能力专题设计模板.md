# Px-y <能力名>设计

> 使用方式：复制为 `docs/NN-Px-y-<主题>设计.md`（L2 能力专题）。
> **篇幅小的改动不另起文档**，避免文档碎片化（`CONTRIBUTING.md` §5）。
> 定位：本文件**自包含**描述单个能力；跨能力共用的契约**不写在这里**，只在「契约归属」节指向 `01`/`02`。

## 1. 背景与目标

<解决什么问题；本能力的边界；上游需求 §/R 编号。>

**上游需求**：§xx / R-xx

## 2. 设计

<状态机、数据结构、时序、关键算法、失败与边界处理。>

### 2.1 <子主题>

### 2.2 <子主题>

## 3. 契约归属（★ 防"撒盐"）

| 契约 | 归属 | 说明 |
|---|---|---|
| 跨能力共用的模型 / 枚举 / 端点 | `02-总体设计.md` §xx | 本文件只引用，不复述 |
| 本能力专属的模型 / 端点 / 审计 | 本文档 | |

## 4. DoD 与自证

- [ ] <可观测、可自证的验收点 1>
- [ ] <验收点 2>
- [ ] 单测覆盖（用例名 / 数量）
- [ ] 回填：阶段计划 §3/§4/§6、RTM、`02`（若涉契约）

**测试证据**：<测试名或命令 + 结果>

## 5. 边界与刻意未做

<已知边界、暂不支持项；每项在 `docs/范围边界清单.md` 登记编号，此处只引编号。>
