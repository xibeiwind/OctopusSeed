# Px 执行计划：<阶段主题>

> 阶段编号依据：<上游收口评审的摘取决议 / 新需求来源>。
> 立项前置：本文件为 **R-Plan-1** 要求的**阶段骨架**；开工前须先由用户决议 §8 的待决议项。
> 使用方式：复制本文件为 `docs/NN-Px执行计划.md`（编号取下一个可用号），**先填 §0~§3 与 §8**，决议后开工。

## 0. 阶段目标

<本阶段要达成的 1~3 条可验收目标，每条可自证。>

## 1. 上游与依赖

| 项 | 来源 | 说明 |
|---|---|---|
| 上游决议 | | |
| 依赖的既有能力 | | |
| 依赖的文档 | | |

## 2. 范围

**占包位（≤8，当前 N/8）**：<列出本阶段工作包编号与名。>

**不占包位（运维项 / 顺带修复 / 结构重构批次，不进包位）**：<列出并在 `范围边界清单.md` 登记；结构重构批次按 `CONTRIBUTING.md` §9.5.2 的入口信号开启，**不参与价值比价**且**总量封顶**。>

## 3. 工作包全景（含依赖与关键路径）★

> **容量上限：8 个包**（`CONTRIBUTING.md` §9 R-Plan-9）。当前 **N 个**，容量 **N/8**。

| 包 | 内容 | 依赖 | 状态 |
|---|---|---|---|
| **Px-1** | | | ⬜ |
| **Px-2** | | | ⬜ |

**关键路径**：<A → B → C>。

**并行机会**：<哪些包互不依赖。>

---

## 4. 当前进度与下一步 ★（**开工入口**）

**已完成（前置）**：<…>

**阶段状态**：<…>

### 下一步：**开工 Px-N（<包名>）**

- <该包的专项设计文档名、主要任务、回填范围、PR 前缀。>

**开工顺序**：<…>

---

## 5. DoD 汇总

- 主门禁：`{{BUILD_CMD}}` **0 警告 0 错误**；`{{TEST_CMD}}` **全绿**（基线 **N**）。
- 其它子项目（前端 / 服务 / 工具链，若存在）：其类型检查、测试与生产构建命令全绿（基线 **N**）。
- <本阶段特有的可自证门禁。>
- 每个包**回填**：本文件 §3/§4/§6、`docs/需求跟踪矩阵.md`、`docs/范围边界清单.md`（若有）、`docs/02`（必要处）。
- 收口时按 **R-Plan-8** 完成清算 / 摘取 / 候选池审视三件事（产出 `docs/NN-Px阶段收口评审.md`）。

---

## 6. 进度记录

（逐包追加：日期 + 包号 + 内容 + 验证数据 + 回填范围 + PR 号。）

- **YYYY-MM-DD · Px-N**：<内容>；`{{TEST_CMD}}` **N 全绿**（含 M 个用例）；回填 <范围>。PR #N。

---

## 7. 刻意未做 / 已知边界

- <每项注明原因与触发条件，并在 `范围边界清单.md` 登记编号。>
- **超出本包验证需要的结构性设计**（`R-Plan-12`）：记在这里，**而不是做掉**；契约性设计不适用本条，它必须一次做对。

---

## 8. 待决议（集中登记，**每条标注归属包**）

> 存在阻塞项时，**先向用户提出决策请求**，不要绕过它继续实施（`CONTRIBUTING.md` §9 R-Plan-5）。

| # | 问题 | 来源 | **阻塞哪个包** | 选项与建议 |
|---|---|---|---|---|
| **Q1** | | | | |

---

## 9. 与既有文档的关系

| 文档 | 关系 |
|---|---|
| `docs/范围边界清单.md` | 唯一登记处（R-Plan-7） |
| `docs/01` / `docs/02` | 跨能力契约；本阶段就地修订 |
| `CONTRIBUTING.md` §9 | R-Plan-1~12 挂载点（含设计分级与结构重构通道） |
