# 需求规格说明书

> **定位（L1 基线）**：**跨能力共用契约**的唯一真相源之一（另一份是 `02-总体设计.md`）。
> 需求 ID 形态：`§<章节号>` 或 `R-<数字>`（功能）／`R-N<数字>`（非功能）——**锁定后不重编号**（`CONTRIBUTING.md` §1）。
> 变更纪律：需求语义因实现而细化 / 收敛时**就地修订**本文件对应条目，并回填 `需求跟踪矩阵.md`（`CONTRIBUTING.md` §5），**不新开编号**。

---

## 1. 背景与目标

<一段话说清：为谁解决什么问题；不写实现方案。>

**可验收目标（≤3 条，每条可自证）**：

1. …（自证方式：…）
2. …
3. …

---

## 2. 范围

**In（本阶段/本项目要做）**：

- …

**Out（红线：明确不做）**：每条都要在 `范围边界清单.md` 登记为**契约级（🔴）**并给出理由——"不做"是决策，不是遗漏。

- …

---

## 3. 功能需求

| 需求 | 描述 | 验收要点 |
|---|---|---|
| R-1 | | |

---

## 4. 非功能需求

| 需求 | 描述 | 验收要点 |
|---|---|---|
| R-N1 | | |

---

## 5. 阶段划分（跨阶段总纲）

| 阶段 | 主题 | 一句话目标 | 状态 |
|---|---|---|---|
| P0 | | | ⬜ |

> **只写一句话定位，不复述要点**（`CONTRIBUTING.md` §9.1 R-Plan-6「上位层不复述下位层」）。
> 阶段内的计划见当前阶段执行计划（`docs/` 下最新的 `NN-Px执行计划.md`）；某个能力的设计见 `NN-Px-y-<主题>设计.md`。

---

## 6. 契约级红线（不许做）

| 编号（`范围边界清单.md`） | 红线 | 理由 |
|---|---|---|
| | | |

> 红线**不属于**艾森豪威尔四象限（不是"不重要"，而是"不许做"），须单独标注，不得混入 Q3/Q4 被误当待办（`CONTRIBUTING.md` §9.5.1）。
