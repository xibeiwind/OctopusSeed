---
description: 阶段计划驱动的推进机制：开工先读当前阶段执行计划 §3/§4，完成工作包后自动回填阶段计划与 RTM。
alwaysApply: true
enabled: true
updatedAt: 2026-09-12T00:00:00.000Z
provider:
---

# 阶段计划驱动（Stage-Plan-Driven）

配合 `CONTRIBUTING.md` §9（**R-Plan-1~12**）。核心原则：**任何"下一步"若没写进文档，就等于不存在。**

## 开工前（必须）

先读 **`docs/` 下最新的 `NN-Px执行计划.md`**（即**当前阶段执行计划**）的——**不写死文件名**：写死会让本规则随阶段推进而陈旧（本项目规范的移植来源曾长期把文件名写死在规则里，落后多个阶段才被发现）：

- §3 工作包全景（含依赖与关键路径）
- §4 当前进度与下一步

并向用户复述三件事：

1. 当前已完成到哪
2. 下一步是什么（含依赖与阻塞项）
3. 哪些待决议阻塞了它

**不要**靠跨文件检索、记忆或猜测来确定"接下来做什么"。

## 完成一个工作包后（必须主动执行，不等用户提醒）

与 RTM 回填**同批提交**，同步更新：

1. 阶段执行计划 §3 **状态列**（改为 ✅ 并附 PR 号）
2. 阶段执行计划 §4 **下一步**（推进到下一个包）
3. 阶段执行计划 §6 **进度记录**（追加一行：日期 + 包号 + 内容 + PR）
4. `docs/需求跟踪矩阵.md` 对应行（见 `CONTRIBUTING.md` §5）
5. 若涉及**跨能力契约**：`docs/01` / `docs/02` 就地修订（不重编号）

## 设计分级（R-Plan-12）

- **契约性设计**（对外接口 / 数据 schema / 持久化格式 / 协议）：**一次做对**；变更走**显式迁移**（阶段计划 §8 登记风险接受 + 给出迁移窗口到期日）。
- **结构性设计**（模块划分 / 内部抽象 / 分层 / 组合方式）：**只做到支撑本包验证即止**，超出部分记入计划 §7 刻意未做。

**不要**为了让结构"看起来更完整"而提前引入抽象或分层——超出本包验证需要的结构性设计属于**刻意未做**，不属于设计质量。设计是**资产**（阶段级生命周期），不是包内的一次性动作。

## 跨阶段时

新阶段立项须**先创建** `docs/NN-Px执行计划.md`（骨架见 R-Plan-1，模板见 `docs/模板/阶段执行计划模板.md`），再开工。

## 阻塞与待决议

- 待决议一律登记到阶段执行计划 §8，并标注**阻塞哪个包**
- 存在阻塞项时，**先向用户提出决策请求**，不要绕过它继续实施

## 总纲三层（引用不复述）

- 跨阶段 → `docs/01` 阶段划分
- 阶段内 → 阶段执行计划
- 包内 → 专项设计 `NN-Px-y-<主题>设计.md`

上位层引用下位层时，**只给能力名 + 一句话定位 + 链接，不摘抄要点**（避免同一事实多份而漂移）。

## 禁止

- 未读阶段执行计划就开工
- 完成工作包后只更新 RTM、漏更阶段执行计划
- 把"下一步"只写在对话里而不落文档
- 对**已跟踪文件整文件重写**——曾有教训：整文件重写 workflow **覆盖了工作区里未提交的改动**，git 侧无法恢复（只能靠 IDE 本地历史）。大改一律用**定向编辑**；确需重写前先提交
