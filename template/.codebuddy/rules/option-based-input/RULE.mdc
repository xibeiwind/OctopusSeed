---
description: 需要用户抉择或输入时一律以结构化选项给出；涉及下一步路线、方案选型、口径确认、待决议拍板时参考。
alwaysApply: false
enabled: true
updatedAt: 2026-09-22T00:00:00.000Z
provider:
---

# 交互选项化（R-Ask-1~5）

> 上位规范见 `CONTRIBUTING.md` §12；本规则是该规范在 AI 侧的**执行方式**，两者不一致时以上位为准。

- **决策请求一律选项化**：凡需要用户抉择 / 输入的场合——下一步路线、方案选型、口径确认、待决议拍板、歧义澄清——必须用结构化选项（`ask_followup_question`）给出，**不要**在正文里用散装问句让用户自由发挥。
- **每条选项附后果 + 标推荐项**：`label` 写结论，`description` 写"选了会怎样"（代价 / 收益 / 影响面）；并在正文标出推荐项与推荐理由。
- **互斥用单选、可并选用多选**：选项**相互排斥**（不同策略、二选一）→ 单选；选项**互不冲突、可同时执行**（跑测试 / 写日志 / 更新 RTM、多个落点并存）→ `multiSelect=true`。不要无脑单选，也不要无脑多选。
- **不许造幻觉入口**：尚不存在的东西（未创建的文档、未验证的技术路径）不能当作可选依赖摆上台面。每问 2~4 个选项；超过说明问题还没拆干净，应先拆而非先问。
- **拍板结果必须落文档**：交互层的选项只是**拍板入口**，不是真相源。结论必须写回**当前阶段执行计划 §8**（`docs/` 下最新的 `NN-Px执行计划.md`，不写死文件名）并把 §4 的阻塞改为可开工——**对话里的一致不等于已决议**，没写进 §8 就不算数（承 `R-Plan-5`）。
- **长期偏好要固化**：用户表达的跨会话偏好，落点至少一处——AI 侧写 `.codebuddy/rules/`，人读侧写 `CONTRIBUTING.md`，跨会话写长期记忆。**说过 ≠ 约定**。

**保障层级**：本规则属**软保障**（`CONTRIBUTING.md` §8.1）——它没有机械判据、不阻断任何动作，**不要假装有门禁**。
