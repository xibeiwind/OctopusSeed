---
description: {{PROJECT_NAME}} 需求/工作包/代码三层编号对齐与 git 提交规范，所有涉及代码改动、提交、分支、PR 时遵循。
alwaysApply: true
enabled: true
updatedAt: 2026-09-12T00:00:00.000Z
provider:
---

# 编号与提交规范（L0 预防层）

本项目用单一编号体系串联「需求 ↔ 工作包 ↔ 代码」，任何代码改动都须对齐：

- **需求**：以 `docs/01-需求规格说明书.md` 的 §章节号 / R-xx 为稳定 ID，**不要**重编号。
- **工作包**：以**当前阶段执行计划**（`docs/` 下最新的 `NN-Px执行计划.md`）的 `Px-x` 为 ID——**不写死文件名**，阶段推进会换。
- **代码改动**：
  - 提交信息首行必须以工作包前缀开头：`[P0-1] 建立核心抽象`。
  - 分支名格式：`Px-x-简短英文描述`（如 `P0-1-core-abstractions`）。
  - **禁止直接提交到 `develop` / `main`**；从集成分支 `develop` 切出特性分支，经 PR 合并。
  - PR 标题前缀同提交；描述填写关联工作包 / 需求(§xx) / DoD / 测试证据。
- 可用 `git log --grep="[P0-1]"` 拉取某工作包全部提交作为完成证据。

> 完整规范见仓库根 `CONTRIBUTING.md` §1–§5。提交信息格式由 `.githooks/commit-msg` 钩子强制校验（启用见 §7）。
