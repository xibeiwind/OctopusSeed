---
description: 需求↔工作包双向追溯与进度回填方式，涉及需求变更、工作包状态、文档更新时参考。
alwaysApply: false
enabled: true
updatedAt: 2026-09-12T00:00:00.000Z
provider:
---

# 进度追溯与需求跟踪矩阵（RTM）

- 需求与工作的双向追溯见 `docs/需求跟踪矩阵.md`（RTM）：需求ID → 工作包 → 验收/证据。
- 新增/变更需求：在 `docs/01-需求规格说明书.md` 加 §/R 编号，并同步 RTM 对应行。
- 进度回填：PR 合并后，在**当前阶段执行计划**（`docs/` 下最新的 `NN-Px执行计划.md`，**不写死文件名**）对应工作包处更新状态（`§3` 表维护「状态」（✅ + PR 号）与「依赖」，或勾选 `§4` 的 `- [ ] / - [x]`），并同步 RTM「状态」列。
- 范围外 / 遗漏项登记见 `docs/范围边界清单.md`（单一登记处，见 `CONTRIBUTING.md` §9.1 R-Plan-7）。
- 完整协作与回填流程见 `CONTRIBUTING.md` §5、§7、§8。

> 本规则为按需参考（非始终加载）；涉及需求变更、状态更新、追溯查询时适用。
